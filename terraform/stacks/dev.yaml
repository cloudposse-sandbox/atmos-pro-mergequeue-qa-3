import:
  - _default.yaml
  - defaults/app.yaml
  - deps/*

vars:
  stage: dev
  deps_stage: dev
  
components:
  terraform:
    app:
      metadata:
        # Override Terraform workspace
        terraform_workspace: "atmos-native-ci-{{ .atmos_stack }}-{{ .atmos_component }}"
